---
export interface Props {
	tag: string
}
const { tag } = Astro.props
---

<a href={`/tags/${tag.toLowerCase()}`} aria-label={tag}>
	<span class='font-semibold text-black text-sm w-fit px-2 py-1 rounded-full'>
		#{tag ?? 'Blog Tag'}
	</span>
</a>
